import React  from 'react';

const Layout= () =>{ 
   return(<header className="App-header"> 
          <h1 className="App-title">Car Inventory Management</h1>
        </header>);
}

export default Layout;